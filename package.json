{
  "name": "color-pallet-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Simple grayscale color palette generator (Vite)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
